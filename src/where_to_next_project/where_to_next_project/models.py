from django.db import models
from django.utils import timezone # takes users timezone into considerations
from django.contrib.auth.models import User # Post model and user model will have relation ship (one to many i.e 1 user -> many posts)
